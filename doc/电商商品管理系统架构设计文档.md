# 电商商品管理系统架构设计文档

## 1. 总体系统架构图

```mermaid
graph TD
    A[客户端] --> B[API网关]:::http
    B --> C[认证授权服务]:::http
    B --> D[商品服务]:::http
    B --> E[库存服务]:::http
    B --> F[供应商服务]:::http
    B --> G[订单服务]:::http
    B --> H[预警服务]:::http
    D --> I[MySQL: 商品主库]:::oltp
    E --> J[MySQL: 库存主库]:::oltp
    F --> K[MySQL: 供应商主库]:::oltp
    E --> L[Redis: 缓存层]:::cache
    D --> M[Elasticsearch: 商品搜索]:::search
    E --> N[RabbitMQ: 事件总线]:::mq
    N --> H:::mq
    N --> O[同步服务]:::http
    O --> P[用友ERP]:::http
    L --> Q[监控系统]:::http
    I --> Q:::http
    J --> Q:::http
    M --> Q:::http
    H --> R[短信/邮件通知]:::http

    classDef http fill:#E3F2FD,stroke:#2196F3,stroke-width:2px;
    classDef oltp fill:#E8F5E8,stroke:#4CAF50,stroke-width:2px;
    classDef cache fill:#FFF8E1,stroke:#FF9800,stroke-width:2px;
    classDef search fill:#F3E5F5,stroke:#9C27B0,stroke-width:2px;
    classDef mq fill:#E0F7FA,stroke:#00BCD4,stroke-width:2px;
```

### 协议标注

- **客户端 → API网关**：HTTP/REST
- **API网关 → 各服务**：HTTP/REST
- **服务 → MySQL**：JDBC
- **服务 → Redis**：Redis Protocol
- **服务 → Elasticsearch**：HTTP/REST
- **服务 → RabbitMQ**：AMQP
- **同步服务 → 用友ERP**：HTTP/REST
- **预警服务 → 短信/邮件通知**：HTTP/REST

### 数据持久层说明

- **OLTP数据库**：MySQL，用于存储商品、库存、供应商等核心业务数据，确保事务一致性
- **缓存层**：Redis，用于缓存热点数据（如商品信息、库存状态），提高系统响应速度
- **搜索引擎**：Elasticsearch，用于商品搜索和动态规格筛选，支持高并发查询

## 2. 功能级执行流程图

### 功能：商品创建与编辑

```mermaid
graph LR
    A[接收创建/编辑请求] --> B{必填字段是否完整?}
    B -- 否 --> C[返回错误信息]
    B -- 是 --> D{商品名称是否重复?}
    D -- 是 --> E[返回ERR_PRODUCT_NAME_DUPLICATE]
    D -- 否 --> F[开启事务]
    F --> G[写入商品基础信息]
    G --> H[关联分类和品牌]
    H --> I[提交事务]
    I --> J[异步更新Elasticsearch索引]
    J --> K[返回SUCCESS]
```

### 功能：商品上下架控制

```mermaid
graph LR
    A[接收上下架请求] --> B{商品是否存在?}
    B -- 否 --> C[返回ERR_PRODUCT_NOT_FOUND]
    B -- 是 --> D{操作权限是否充足?}
    D -- 否 --> E[返回ERR_PERMISSION_DENIED]
    D -- 是 --> F[更新商品状态]
    F --> G[异步更新Elasticsearch索引]
    G --> H[返回SUCCESS]
```

### 功能：商品分类管理

```mermaid
graph LR
    A[接收分类操作请求] --> B{分类名称是否重复?}
    B -- 是 --> C[返回ERR_CATEGORY_NAME_DUPLICATE]
    B -- 否 --> D{是否为删除操作?}
    D -- 是 --> E{是否存在子分类?}
    E -- 是 --> F[禁止删除并返回错误]
    E -- 否 --> G[删除分类]
    D -- 否 --> H[创建/更新分类]
    G --> I[返回SUCCESS]
    H --> I
```

### 功能：品牌管理

```mermaid
graph LR
    A[接收品牌操作请求] --> B{品牌名称是否重复?}
    B -- 是 --> C[返回ERR_BRAND_NAME_DUPLICATE]
    B -- 否 --> D[创建/更新品牌]
    D --> E[返回SUCCESS]
```

### 功能：实时库存同步

```mermaid
graph LR
    A[接收库存变更请求] --> B{库存是否充足?}
    B -- 否 --> C[返回ERR_INVENTORY_INSUFFICIENT]
    B -- 是 --> D[开启事务]
    D --> E[更新库存数量]
    E --> F[提交事务]
    F --> G[发送库存变更事件到RabbitMQ]
    G --> H[同步服务消费事件]
    H --> I{同步是否成功?}
    I -- 否 --> J[记录异常并重试]
    I -- 是 --> K[返回SUCCESS]
```

### 功能：多仓库存管理

```mermaid
graph LR
    A[接收库存调整请求] --> B{仓库是否存在?}
    B -- 否 --> C[返回ERR_WAREHOUSE_NOT_FOUND]
    B -- 是 --> D{SKU是否存在?}
    D -- 否 --> E[返回ERR_SKU_NOT_FOUND]
    D -- 是 --> F[更新仓库库存]
    F --> G[返回SUCCESS]
```

### 功能：库存预警

```mermaid
graph LR
    A[库存变更事件] --> B{库存是否低于阈值?}
    B -- 否 --> C[结束]
    B -- 是 --> D[发送预警事件到RabbitMQ]
    D --> E[预警服务消费事件]
    E --> F[发送短信/邮件通知]
    F --> G{通知是否成功?}
    G -- 否 --> H[记录日志并重试]
    G -- 是 --> I[结束]
```

### 功能：多维度SKU管理

```mermaid
graph LR
    A[接收SKU生成请求] --> B{属性是否冲突?}
    B -- 是 --> C[返回ERR_ATTR_CONFLICT]
    B -- 否 --> D{SKU数量是否超过限制?}
    D -- 是 --> E[返回ERR_SKU_LIMIT_EXCEEDED]
    D -- 否 --> F[生成SKU列表]
    F --> G[写入SKU信息]
    G --> H[返回SUCCESS]
```

### 功能：动态规格筛选

```mermaid
graph LR
    A[接收筛选请求] --> B{筛选条件是否有效?}
    B -- 否 --> C[返回ERR_FILTER_INVALID]
    B -- 是 --> D[构建Elasticsearch查询]
    D --> E[执行搜索]
    E --> F[返回符合条件的商品列表]
```

### 功能：供应商信息管理

```mermaid
graph LR
    A[接收供应商操作请求] --> B{供应商名称是否重复?}
    B -- 是 --> C[返回ERR_SUPPLIER_NAME_DUPLICATE]
    B -- 否 --> D{税号格式是否正确?}
    D -- 否 --> E[返回ERR_TAX_ID_INVALID]
    D -- 是 --> F[创建/更新供应商]
    F --> G[返回SUCCESS]
```

### 功能：供应商商品关联

```mermaid
graph LR
    A[接收关联请求] --> B{供应商是否存在?}
    B -- 否 --> C[返回ERR_SUPPLIER_NOT_FOUND]
    B -- 是 --> D[建立商品与供应商的关联]
    D --> E[返回SUCCESS]
```

### 功能：批量导入商品

```mermaid
graph LR
    A[接收导入请求] --> B{文件格式是否正确?}
    B -- 否 --> C[返回ERR_FILE_FORMAT_INVALID]
    B -- 是 --> D{数据格式是否正确?}
    D -- 否 --> E[返回ERR_DATA_FORMAT_INVALID]
    D -- 是 --> F[批量处理商品数据]
    F --> G[返回导入成功/失败信息]
```

### 功能：批量调整价格/库存

```mermaid
graph LR
    A[接收批量操作请求] --> B[批量处理商品]
    B --> C{是否全部成功?}
    C -- 是 --> D[返回SUCCESS]
    C -- 否 --> E[返回ERR_PARTIAL_OPERATION_FAILED]
```

### 功能：异常预警

```mermaid
graph LR
    A[接收异常事件] --> B[发送预警事件到RabbitMQ]
    B --> C[预警服务消费事件]
    C --> D[发送短信/邮件通知]
    D --> E{通知是否成功?}
    E -- 否 --> F[记录日志并重试]
    E -- 是 --> G[结束]
```

### 功能：多仓库存分配

```mermaid
graph LR
    A[接收分配请求] --> B[获取各仓库库存情况]
    B --> C{是否有仓库库存充足?}
    C -- 否 --> D[返回ERR_ALL_WAREHOUSE_INSUFFICIENT]
    C -- 是 --> E[根据订单地址和库存情况分配最优仓库]
    E --> F[返回分配结果]
```

### 功能：多仓库存同步

```mermaid
graph LR
    A[接收库存同步请求] --> B[更新仓库库存]
    B --> C[发送同步事件到RabbitMQ]
    C --> D[同步服务消费事件]
    D --> E{同步是否成功?}
    E -- 否 --> F[记录异常并重试]
    E -- 是 --> G[返回SUCCESS]
```

## 3. 技术选型对比与最优理由

| 技术组件 | 选项A | 选项B | 选项C | 最优选型 | 选择理由 |
|---------|-------|-------|-------|----------|----------|
| 缓存层 | Redis | Memcached | Apache Ignite | Redis | 支持持久化、数据结构丰富（Hash/List/ZSet）、单机QPS > 10万、支持Lua脚本实现原子锁，满足库存锁仓与动态属性缓存需求；Memcached无持久化，Ignite过重且运维复杂 |
| 消息队列 | RabbitMQ | Kafka | RocketMQ | RabbitMQ | 业务场景为低延迟、高可靠事件通知（库存变更、ERP同步），RabbitMQ支持优先级队列、TTL、死信交换，更适合事务型异步解耦；Kafka吞吐高但延迟高，RocketMQ企业级功能冗余 |
| 搜索服务 | Elasticsearch | Solr | MySQL全文索引 | Elasticsearch | 支持中文分词、动态映射、聚合分析，可实时索引SKU属性变更；Solr配置复杂，MySQL全文索引不支持高并发与模糊匹配 |
| 数据库 | MySQL | PostgreSQL | TiDB | MySQL | 事务强一致、生态成熟、与现有ERP系统兼容性最佳；PostgreSQL功能强但运维成本高，TiDB为分布式场景冗余，当前SKU量级<5000万，单机MySQL可支撑 |
| API网关 | Spring Cloud Gateway | Kong | Zuul | Spring Cloud Gateway | 与Spring Cloud生态无缝集成，支持动态路由、限流、熔断，性能优于Zuul，配置简单；Kong功能丰富但部署复杂 |
| 服务框架 | Spring Boot | Quarkus | Micronaut | Spring Boot | 生态成熟、社区活跃、与现有技术栈兼容性最佳，开发效率高；Quarkus和Micronaut启动快但生态相对薄弱 |
| 监控系统 | Prometheus + Grafana | Zabbix | ELK Stack | Prometheus + Grafana | 开源、轻量级、支持多维度监控和告警，与Spring Boot集成良好；Zabbix配置复杂，ELK Stack更适合日志分析 |
| 分布式事务 | Seata | TCC-Transaction | Atomikos | Seata | 支持AT、TCC、SAGA等多种模式，与Spring Cloud集成良好，性能优于其他方案；TCC-Transaction实现复杂，Atomikos主要用于JTA |
| 对象存储 | OSS | S3 | MinIO | OSS | 与阿里云生态集成良好，支持CDN加速，适合存储商品图片和视频；S3需额外配置，MinIO需要自建维护 |
| 容器编排 | Kubernetes | Docker Swarm | Mesos | Kubernetes | 生态成熟、社区活跃、功能丰富，支持自动扩缩容和服务发现；Docker Swarm功能简单，Mesos学习成本高 |